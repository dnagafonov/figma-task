import React from 'react'
import {workersList} from '../workers/worker-list'

export default class Timetable extends React.Component{

    state = {
        workers: workersList
    }

    render() {
        return true;
    }
}